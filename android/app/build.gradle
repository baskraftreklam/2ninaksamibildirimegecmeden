// C:\Dev\TalepifyApp\android\app\build.gradle

apply plugin: "com.android.application"
apply plugin: "org.jetbrains.kotlin.android"
apply plugin: "com.facebook.react"

/**
 * Bu blok, React Native Android uygulamanı özelleştirir.
 * Varsayılanlarla çalışır; gerekirse yorum satırlarını açıp kullanabilirsin.
 */
react {
    /* Örnek: debuggableVariants, bundle ayarları vs. (şimdilik gerek yok) */
}

def enableProguardInReleaseBuilds = false

// JSC (Hermes kapalıysa kullanılır)
def jscFlavor = 'org.webkit:android-jsc:+'

android {
    ndkVersion rootProject.ext.ndkVersion
    buildToolsVersion rootProject.ext.buildToolsVersion
    compileSdk rootProject.ext.compileSdkVersion

    namespace "com.talepifyapp"

    defaultConfig {
        applicationId "com.talepifyapp"   // Firebase’te eklediğin Android package name ile birebir aynı olmalı
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 1
        versionName "1.0"
    }

    signingConfigs {
        debug {
            storeFile file('debug.keystore')
            storePassword 'android'
            keyAlias 'androiddebugkey'
            keyPassword 'android'
        }
    }

    buildTypes {
        debug {
            signingConfig signingConfigs.debug
        }
        release {
            // Üretimde kendi release keystore’unu kullan.
            signingConfig signingConfigs.debug
            minifyEnabled enableProguardInReleaseBuilds
            proguardFiles getDefaultProguardFile("proguard-android.txt"), "proguard-rules.pro"
        }
    }
}

dependencies {
    // RN sürümüne uygun android bağımlılığı; sürümü RN Gradle Plugin belirler
    implementation("com.facebook.react:react-android")

    // Hermes açık/kapalı durumuna göre motor seçimi
    if (hermesEnabled.toBoolean()) {
        implementation("com.facebook.react:hermes-android")
    } else {
        implementation jscFlavor
    }
}

// Otomatik native module linkleme
apply from: file("../../node_modules/@react-native-community/cli-platform-android/native_modules.gradle")
applyNativeModulesAppBuildGradle(project)

// 🔴 Firebase Google Services plugin (EN ALTA KALACAK)
apply plugin: 'com.google.gms.google-services'
